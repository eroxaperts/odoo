name: Odooy

on:
  push:
    branches: 
      - main

jobs:
  my-testing:
    runs-on: ubuntu-latest
    steps:
      
    - name: Print hello
      run : echo "hello"
  my-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Print hell
      run : echo "hello my-deploy"
